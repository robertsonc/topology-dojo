/**
 * User-facing copy for public share links. Shared by the MCP publish tool
 * description and the /v/:id viewer banner so the warning cannot drift.
 */
export const SHARE_PUBLIC_WARNING =
  'This link is public: anyone with the URL can view the snapshot for up to 30 days, without signing in.';
